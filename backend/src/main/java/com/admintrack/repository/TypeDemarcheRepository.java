package com.admintrack.repository;

import com.admintrack.model.TypeDemarche;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TypeDemarcheRepository extends JpaRepository<TypeDemarche, Long> {
}
